<!DOCTYPE html>
<html lang="en">
<head>
    <title>Location Index List</title>
    <meta charset="UTF-8">
    <meta name="description" content="Location Index List">
    <meta name="author" content="Beren Scott">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
</head>
<body>
<nav class="topheading">
    <% if (user.itemR) {%><a href="/item/">New Items</a><%}%>
    <% if (user.locationR) {%><a href="/location/">Locations</a><%}%>
    <% if (user.jobR) {%><a href="/job/">Jobs</a><%}%>
    <% if (user.itemR) {%><a href="/report/find">Find Items</a><%}%>
    <% if (user.wipeR) {%><a href="/wipe/status">Wipe</a><%}%>
    <% if (user.admin) {%><a href="/price/">Pricing</a><%}%>
    <% if (user.admin) {%><a href="/user/">User</a><%}%>
    <% if (user.admin) {%><a href="/admin/">Admin</a><%}%>
    <a href="/logout">Logout</a>
</nav>
<div class="container">
    <img src="/bshw-big.png" alt="BSHW banner">
    <h1>Location List - <%= many.length%> items</h1>
    <% if(many.length>0) { %>
        <table class="list-table">
            <thead>
                <tr>
                    <th><a href="/location/create">Add</a></th>
                    <th>Location</th>
                    <th>Item Type</th>
                    <th>Notes</th>
                    <th>Item #</th>
                </tr>
            </thead>
            <tbody>
            <% for(var i=0; i<many.length;i++) { %>
                <tr<%if (many[i].locked) {%> style="background-color: <% if((i+1) % 2 == 0) { %>#eab676"<%} else {%>#f0cc9f"<%} }%>>
                    <td><a href="/location/show/<%= many[i]._id%>">
                            <img src="/bshw.png" alt="atr" width="30" height="15"></a></td>
                    <td><%= many[i].locate%></td>
                    <td><%= many[i].itemType%></td>
                    <td><%= many[i].notes%></td>
                    <td><a href="/report/location/<%= many[i]._id%>"><%= many[i].itemList.length%></a></td>
                </tr>
            <% } %>
            </tbody>
        </table>
    <% } else { %>
        <div><a href="/location/create">Add</a></div>
    <% } %>
</div>
</body>
</html>